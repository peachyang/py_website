<?php
$methods = $this->getPaymentMethods();
$current = $this->getCurrentMethod();
?>
<section class="section payment">
    <h4 class="title"><?php echo $this->translate('Payment Method') ?></h4>
    <?php if ($methods): ?>
        <ul class="list list-inline">
            <?php foreach ($methods as $code => $model): ?>
                <li>
                    <input type="radio" name="payment_method" id="payment-method-<?php echo $code ?>" value="<?php echo $code ?>"<?php if ($code === $current || count($methods) === 1): ?> checked="checked"<?php endif ?> />
                    <label for="payment-method-<?php echo $code ?>">
                        <?php echo $this->translate($model->getLabel()) ?>
                    </label>
                    <a href="#payment-description-<?php echo $code ?>" data-toggle="tab" hidden="hidden"></a>
                </li>
            <?php endforeach ?>
        </ul>
        <div class="tab-content">
            <?php foreach ($methods as $code => $model): ?>
                <div class="tab-pane fade<?php if ($code === $current || count($methods) === 1): ?> active in<?php endif ?>" id="payment-description-<?php echo $code ?>">
                    <?php echo $this->translate($model->getDescription()) ?>
                </div>
            <?php endforeach ?>
        </div>
    <?php else: ?>
        <p><?php echo $this->translate('Sorry, no payment methods are available for this order at this time.') ?></p>
    <?php endif ?>
</section>
