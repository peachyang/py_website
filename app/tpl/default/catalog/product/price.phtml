<?php
$product = $this->getVariable('product');
$currency = $this->getCurrency();
$format = $currency['format'];
$symbol = $currency['symbol'];
?>
<?php if ($product->getFinalPrice() == $product['prices']['regular']): ?>
    <span class="regular-price"><?php printf($format, $symbol, $product['prices']['regular']) ?></span>
<?php else: ?>
    <span class="old-price"><?php printf($format, $symbol, $product['prices']['regular']) ?></span>
    <span class="special-price"><?php printf($format, $symbol, $product->getFinalPrice()) ?></span>
<?php endif; ?>
